# Supabase credentials for Party Mode (networked play). Optional: without
# them the app still runs — Pass & Play is unaffected and Party Mode shows a
# "not configured" notice. See supabase/README.md for the full setup.
#
# Copy to .env.local and fill in the anon key from
# Supabase Dashboard → Settings → API (project igfbgvokozpmawwveivc).
VITE_SUPABASE_URL=https://igfbgvokozpmawwveivc.supabase.co
VITE_SUPABASE_ANON_KEY=
